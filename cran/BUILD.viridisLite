load("@com_grail_rules_r//R:defs.bzl", "r_pkg")

package(default_visibility = ["//visibility:public"])

r_pkg(
    name = "viridisLite",
    srcs = glob(
        ["**"],
        exclude = [],
    ),
    deps = [],
    lazy_data = True,
)
